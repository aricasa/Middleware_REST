package it.polimi.rest.authorization;

/**
 * Entity that can access to the protected resource (if previously allowed to).
 *
 * The classes implementing this interface must override the
 * {@link #equals(Object)} method and provide consistent logic.
 */
public interface Agent {

}
